import { createMessageDiagnosis } from "./message_diagnosis/message-diagnosis";
const message: string = "I genuinely hate myself. I cannot overstate it, I just genuinely hate myself. I do not like pandas, like at all. I do not like pandas. I really do not like pandas.";

console.log(createMessageDiagnosis(message));